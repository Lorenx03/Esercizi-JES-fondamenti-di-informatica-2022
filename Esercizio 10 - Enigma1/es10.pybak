file = pickAFile()

pic = makePicture(file)

color = makeColor(255,0,0)

def enigma1(pict, col):  
# @param pict: picture  
# @param col: color 
  for x in range(0, 400, 1): 
    px = getPixel(pict, x, x)  
    setColor(px, col)
    print x
    
  show(pict)

  
  