
file= pickAFile()
pic= makePicture(file)
metaLarghezza= getWidth(pic)/2 
metaAltezza=getHeight(pic)/2

def verticalStrips(pic,metaLarghezza):
#@param pic: Picture
#@param metaLarghezza: Float
  for p in range (0, getWidth(pic), 2*metaLarghezza):
    print 'half width ', metaLarghezza
    print 'width ', getWidth(pic)
   
  
verticalStrips(pic,metaLarghezza)
